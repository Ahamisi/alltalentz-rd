export const metadata = {
  title: 'Request Talent',
  description: 'Discover Great Talent Offers and Save on Payroll with All Talentz',
  alternates: { canonical: 'https://alltalentz.com/request-talent' },
};

export default function RequestTalentLayout({ children }) {
  return children;
}
